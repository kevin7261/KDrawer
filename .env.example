# 本機開發：複製為 .env 後填入（勿 commit）
# 線上 GitHub Pages：請用 .env.production（可 commit），因 vite build 只會在建置時讀入並寫進靜態檔；僅設本機 .env 而沒有 .env.production 時，在別台或未帶環境時 build 會變成「雲端未設定」。
# OAuth「網頁應用程式」的 Client ID 會出現在瀏覽器，不要放 client secret。
VITE_GOOGLE_CLIENT_ID=your-google-oauth-client-id.apps.googleusercontent.com
